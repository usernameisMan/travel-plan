---
description: 
globs: 
alwaysApply: true
---
# 🛡️ Next.js 项目 AI 修改保护规则

## 🧠 项目说明

这是一个基于 Next.js 的项目，包含多个页面、API 路由、组件和服务逻辑。当前项目已上线或具备完整功能，因此：

> ❗AI 不能破坏现有功能  
> ✅只允许小心地添加新功能或优化已有代码结构

---

## 📌 AI 写代码时必须遵守以下规则：

1. **不能删除任何导出函数、组件、API 路由或关键业务逻辑**。
2. **添加功能必须保持向后兼容**，例如页面路径、接口字段不可更改。
3. **不要更改组件、Hook、API 返回值结构，除非我确认。**
4. **不要重构现有模块，只允许添加代码或注释说明。**
5. **不允许将 SSR 页面变成 CSR，也不允许把 Server Actions 改成 API route，除非我说可以。**
6. **新增页面必须路由不冲突，且命名规范。**
7. **不能擅自使用 unstable_ API。**
8. **对数据处理相关逻辑（如数据库操作、缓存逻辑）只能增强，不能删减。**
9. **对 createTravelPlan 页面的修改必须保证代码现有功能逻辑，非侵入式修改，如果需要修改必须得到我的同意**

---

## 🧱 推荐写法

- 保持模块职责分离，例如：
  - API 逻辑放在 `/pages/api/` 或 `app/api/` 中
  - UI 组件放在 `/components/`
  - 工具函数放在 `/lib/` 或 `/utils/`

- 增加功能时，应尽可能复用已有组件或 Hook

- 若有新的复杂逻辑，请新建文件或新建 Hook，而不要直接嵌进已有代码

---

## ❓不确定时

当你（AI）不确定某个模块能否改动，请**提示我而不是直接修改**，比如：

> “是否允许我重构 `/pages/index.tsx` 中的 fetch 逻辑？目前逻辑可能重复。”

---

## ✅ 样例提示（用于 AI 补全参考）

> ✅ 合格：“新增一个 `/pages/about.tsx` 页面，不影响首页与现有路由”  
> ❌ 不合格：“重写了 `/pages/index.tsx`，替换了页面结构”  

---

请像在手术室操作一个上线项目一样谨慎。
