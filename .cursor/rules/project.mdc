---
description: 
globs: 
alwaysApply: true
---
# 💡 项目保护规则（Project Integrity Rules）

## 1. 核心原则

- 不允许破坏或移除现有功能，除非我明确指示。
- 所有新功能的添加必须是“非侵入式”的，即不能修改现有功能的行为。
- 请小心处理现有代码中的逻辑，尤其是涉及用户数据、API 响应、核心模块（如 xxx、yyy、zzz）的部分。
- 需要拆分组建或者模块时候一定要做到

## 2. 文件和模块的保护

## 3. 对 AI 的要求

- 每次对代码的修改都应**最小化变更范围**。
- 在做出任何重构前，请先提出理由。
- 添加新代码时，请尽量贴近现有代码风格。
- 如果无法确定某段代码的作用，请不要随意修改，而是标记出来我来确认。

## 4. 常见坑（你不能犯）

- 不要删除任何正在使用的导出函数。
- 不要修改 API 接口返回格式。
- 不要将业务逻辑从 Controller 移动到 Service（保持职责分明）。
- 不要随意使用 `any`、`as unknown`，会污染类型系统。

---

🛡️ 总之，请像维护一个“正在运行的线上系统”那样小心谨慎。
